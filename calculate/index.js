const calculateSum = require("./sum");
const { calculateSub, enter } = require("./sub");
const { calculateMultiply } = require("./multiply");

module.exports = {
    calculateSum,
    calculateSub,
    enter,
    calculateMultiply
};
